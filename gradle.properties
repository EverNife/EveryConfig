# Toolchains used by the build and by the -PtestJdk multi-runtime test runs. Auto-download is off so the
# build only ever uses JDKs already installed locally (no surprise network fetch).
org.gradle.java.installations.auto-download=false
