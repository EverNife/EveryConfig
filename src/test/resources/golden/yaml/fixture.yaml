# EveryConfig golden fixture
# locks the emitted layout

# the display name
name: demo
server:
  host: localhost
  port: 25565 # listen port

tags:
  # primary tag
  - alpha
  - beta
  - gamma
accounts:
  alice:
    balance: 100
  bob:
    balance: 50

# end of fixture
